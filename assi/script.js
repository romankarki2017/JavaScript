// Concatenate first name and last name
function getFullName(firstName, lastName) {
  return firstName + " " + lastName;
}
// Basic function that says hello
function sayHello() {
  alert("Hello!");
}

var fullName = getFullName("Roman", "Karki");
alert("Full Name: " + fullName);

sayHello();